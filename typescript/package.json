{
  "name": "median_list_of_lists",
  "version": "1.0.0",
  "description": "Median of a List of Lists (with no concatenation)",
  "author": "Murex",
  "license": "MIT",
  "devDependencies": {
    "@types/jest": "*",
    "jest": "*",
    "ts-jest": "*",
    "jest-junit": "*",
    "typescript": "*"
  },
  "scripts": {
    "test": "jest --watchAll"
  },
  "jest-junit": {
    "outputDirectory": "./test_results"
  }
}
